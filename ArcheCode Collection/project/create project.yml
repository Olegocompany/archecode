info:
  name: create project
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/projects/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
