info:
  name: create project
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/projects/"
  body:
    type: json
    data: |-
      {
        "name": "какая",
        "description": "one more tryууу",
        "link": "github.com/Olegocompany/archecode.git"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
