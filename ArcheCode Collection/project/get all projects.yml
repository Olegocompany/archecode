info:
  name: get all projects
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/projects/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
