info:
  name: Update project
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}/projects/1"
  body:
    type: json
    data: |-
      {
        "description": "калл какой-та"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
