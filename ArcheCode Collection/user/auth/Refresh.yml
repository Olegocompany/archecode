info:
  name: Refresh
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/auth/refresh"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
